/**
 * Placeholder entry point. `W0-T04` replaces this with the Vite/React shell: router, layout,
 * i18n (ES/EN) and theme tokens. It exists now only so that the workspace has a member under
 * `apps/` whose resolution of `@marketplace/config` can be tested.
 */
export const name = '@marketplace/web';
