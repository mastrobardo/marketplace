{
  "name": "@marketplace/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Vite + React SPA. Skeleton only — W0-T04 brings the router, layout, i18n and theme.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@marketplace/config": "workspace:*",
    "@types/node": "^24.7.2",
    "eslint": "^10.10.0",
    "typescript": "~5.9.3",
    "vitest": "^5.0.0"
  }
}
