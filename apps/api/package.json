{
  "name": "@marketplace/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Fastify API — health, config, error envelope, request-id and structured logging.",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@marketplace/config": "workspace:*",
    "@types/node": "^24.7.2",
    "eslint": "^10.10.0",
    "tsup": "^8.5.1",
    "tsx": "^4.23.13",
    "typescript": "~5.9.3",
    "vitest": "^5.0.0"
  },
  "dependencies": {
    "fastify": "^5.12.3",
    "zod": "^4.5.4"
  }
}
