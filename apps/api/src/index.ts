/**
 * Placeholder entry point. `W0-T03` replaces this with the Fastify app: health route, config
 * loading, error envelope, request-id and pino logging. It exists now only so that the workspace
 * has a member under `apps/` whose resolution of `@marketplace/config` can be tested.
 */
export const name = '@marketplace/api';
