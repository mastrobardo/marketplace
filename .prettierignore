dist/
.turbo/
coverage/
.vitest/
pnpm-lock.yaml

# Prose is hand-wrapped at 100 columns and `.claude/agents/**` is generated by
# scripts/generate-claude-agents.ts — reformatting either breaks the `agents-drift` gate
# and makes doc diffs unreadable.
**/*.md
