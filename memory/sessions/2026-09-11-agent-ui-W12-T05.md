---
task:    W12-T05
agent:   agent-ui
session: 2026-09-11T17:40:00Z
status:  closed
---

# Session — W12-T05

## Goal
Three token layers, `[data-theme]` switching, a second theme that proves the swap, and a
`tokens.test.ts` that checks layering and per-theme completeness. The last W12 foundation task
before the search-schema seam (`T07`/`T08`).

## Current state
Branch `W12-T05-token-layers-and-themes`, cut from `main` at `8ae587f`. **Not stacked** — the
standing rule after three PRs read MERGED while `main` never got the code.

## Log
- 17:41 Read the board before proposing anything: `T01`–`T04` and `T06` merged, `T05` open as #205,
  and `TODO.md` had not been marked for any of them. Marked all six in this branch.
- 17:44 Plan put to the operator, including the one call that could have gone either way: a
  high-contrast second theme rather than a decorative second brand. Approved as proposed.
- 17:52 Amended my own plan before implementing it. `prefers-contrast: more` cannot apply a whole
  theme without duplicating the semantic mapping — plain CSS cannot combine a media query with an
  attribute selector in one rule. The media query now carries `--mp-border-width` and
  `--mp-focus-ring-width` only; picking a theme from a preference is `W12-T09`'s.
- 17:55 The same problem one layer down had a better answer: `color-scheme` + `light-dark()` writes
  each colour **once** with both schemes in it, instead of a dark palette declared twice. Two rules
  replace a second copy of the file.
- 17:58 Red run found three test bugs before it found any CSS bug — `--trigger-width` is React
  Aria's, a theme's palette resolves only inside that theme, and the scheme selector must not be
  anchored to `:root` or the four-up story is four copies of the document.
- 18:00 106 green (64 unit + 42 storybook). Read the four backgrounds before believing the story:
  a story rendering four identical panels would also have passed on the first run.
- 18:02 Checked the built CSS rather than assuming: Lightning CSS lowers `light-dark()` to its own
  space-toggle and keys it off both the media query and the explicit `color-scheme`, so
  `[data-scheme]` survives the build — but `light-dark(` appears zero times in `dist`.

## Handoff
`pnpm verify` whole, `pnpm build:storybook` clean. Nothing pending.

Next in `W12`: **T07** (the search bar as a declarative schema) and **T08** (the contract request to
`agent-contracts` for `SearchQuery`/`SearchResult`, with MSW handlers built from the
`packages/testing` factories — never a second fixture set). T07's patterns should need no new
colour; if one does, it now needs a semantic token in **both** themes, and that is a failing test
rather than a conversation.
