---
task:    W12-T03
agent:   agent-ui
session: 2026-09-11T15:42:00Z
status:  closed
---

# Session — W12-T03

## Goal
Storybook 10 in `packages/ui`, theme and locale toolbars, and every story running as a Vitest
browser test in the runner the repo already has. Third of the queued series.

## Current state
Branch `W12-T03-storybook-workbench`, stacked on `W12-T02-ui-primitives` (PR #219) which is stacked
on `W12-T01` (PR #218). Both open.

## Log
- 15:45 `@vitest/browser-playwright` has no default export in Vitest 5 — `import { playwright }`.
- 15:50 Storybook ≥10.3 applies `preview.tsx`'s annotations to the browser project itself and says
  so on every run when a setup file does it too. Deleted `.storybook/vitest.setup.ts`. Every
  tutorial older than 10.3 says to write that file.
- 15:52 Disabled Storybook telemetry (`core.disableTelemetry`). It phones home on dev, build and
  test; this repo is personal and most runs here are an agent's.
- 15:55 Narrowed the jsdom story test: the browser project renders every story properly now, so the
  jsdom copy asserted the same thing twice in the weaker environment. What stays is the part a
  browser cannot do — fail when a `LongText` story was never written.
- 15:58 **`unit` is red on `main`**, and has been since #195 and #196 merged in that order:
  `AuditRecord` (W1-T07) has no factory, and W1-T09's gate requires one. Confirmed by stashing this
  branch entirely. Not mine to fix — `packages/testing` is `agent-contracts`', and the right answer
  is a design call between a builder and the allowlist entry the gate's own message offers.
  **The turbo cache hid it**: that test reads `apps/api/prisma/schema.prisma`, which is not part of
  the hash for `@marketplace/testing#test`, so a stale pass was replayed until an install
  invalidated it.

## Handoff
Green except the pre-existing failure above: 79 tests in `packages/ui` (39 jsdom + 40 Chromium),
`storybook build` produces a static site, `pnpm verify` otherwise whole.

**W12-T04 is done** (branch `W12-T04-story-a11y-gate`): the addon and the global parameter were the
entire wiring, and axe found **nothing** across 38 stories — React Aria doing the expensive half,
which is the first evidence for ADR-012 §2's bet rather than an argument about it. The gate was
proven by a deliberate probe (an `img` with no `alt`, a `button` with no text) that failed the run
and was then deleted; what remains is an assertion that the severity is still `'error'`, because
`'todo'` is one word away.

For **W12-T06** (#206): `pnpm --filter @marketplace/ui build:storybook` → `storybook-static/`
(8.7 MB, git-ignored). Mirror `deploy-preview.yml`, including its rule of reading the URL back from
wrangler rather than constructing it. The Pages project `marketplace-ui` **will not exist** — that
is a human's account action, and the reason T06 is `[M]` rather than `[A]`.

For **W12-T05**: the toolbar already puts `[data-theme]` on the story container *and* the document
root (overlays portal to `document.body`). You need a palette, not a workbench change.
