---
task:    W4-T02
agent:   agent-jobs
session: 2026-09-20T14:10Z
status:  closed
---

# Session — W4-T02

## Goal

Give `OPEN` an exit, and pay the two debts `W4-T01` left by name (`MEM-2026-09-20-11`):

1. Remove `OPEN` from `jobMachine.terminal` **in the same change** that gives it an exit.
2. Move "only a draft may be edited" out of `repository.update`'s hand-written `if` and into
   something the states themselves enforce.

Scope settled by the operator before any file was touched: **cancel now, award with quotes.**
`CANCELLED` is the only state this ticket can honestly produce — `AWARDED` has no producer until
`W4-T03`/`W4-T05`, and `IN_PROGRESS`/`COMPLETED` belong to the booking lifecycle in `W5`.

## Current state

Branch `W4-T02-job-cancellation`, **stacked on `docs-w4t02-next` (PR #276, open)** rather than on
`main`: that PR carries `MEM-2026-09-20-11` and the `TODO.md` banner this ticket updates, so basing
on `main` would have meant resolving the same two files twice.

Starting point: `W4-T01` green on main — contracts 23/23, api 405/405 under `STACK_LIVE=1`.

## Log

- 14:05 operator decision 1 — **cancel now, award with quotes**. Rejected the full six states:
  three of them (`IN_PROGRESS`, `COMPLETED`, and `CANCELLED`-after-award) are money's, not jobs'.
- 14:07 operator decision 2 — **an `OPEN` job stays uneditable**. Amendment is a real feature that
  has to invalidate quotes, and quotes do not exist yet.
- 14:20 operator raised `GET /api/jobs/me` → **`GET /api/me/jobs`**. They were right and the repo
  already said so: `W2-T03` §3.7 writes *both* spellings in one code block — `app.put('/providers/me')`
  for a singleton, `app.get('/me/addresses')` for a collection. `jobs` is a collection, so `W4-T01`
  broke the pattern. Zero callers in `apps/web`, so the rename costs nothing today and gets more
  expensive with every slice that copies it (`W4-T03`, `W4-T06`, `W6`).

- 14:40 **the contract refused nothing this time** — `defineMachine` accepted the table on the first
  run. The first red was `tsc`, which is the finding: `Record<JobStatus, boolean>` fails the build
  when a state is added, and that was proven by adding `AWARDED` and reverting, not asserted.
- 14:55 `JobContext` replaced `JobPublishContext`. One context type serves the whole machine, so
  `CANCEL` must be handed one — it loads the real `categoryCount` rather than a `0` that is not true.
- 15:00 added a live test that shells out to `psql` and proves `0011`'s rollback **refuses** when a
  cancelled row exists. `db.test.ts` AC10 only ever rolls back an empty database, so nothing else
  would have caught a guard that did not fire.

## Blocked / escalations

None.

## Handoff

**Next action:** `W4-T03` — quotes. It inherits the question `W4-T01` §2.4 refused to prejudge:
**a three-trade job — one quote covering everything, or three covering parts?** Ask the operator
before modelling; the join table does not decide it either way.

**Raise with `agent-money` before either slice writes a state:** does a job track `IN_PROGRESS` and
`COMPLETED`, or read them off its `Booking`? `MEM-2026-09-20-14`. A job reading `COMPLETED` while
its booking reads `DISPUTED` costs a refund.

**Do not redo:**
- The state scope. `CANCELLED` only, and the three missing states have named owners (spec §6.1).
- The route spelling. Own-scoped collections are `/api/me/<collection>` (`MEM-2026-09-20-12`).
- The edit rule. It is `canEditJob` in the contract; do not re-add a status comparison at a call
  site, and do not reach for an `EDIT` self-transition — `MEM-2026-09-20-13` says why.
- The cancellation reason. It is audit metadata. No column (`MEM-2026-09-20-15`).
- `CANCELLED` being terminal. Deliberate; the counter-argument is in the run record's
  self-assessment for whoever wants to reopen it.

**Known weakness, stated in the run record:** `JobStatusSchema` and `jobMachine.states` can still
drift — a new enum member fails the compiler at `EDITABLE_IN` but *not* at the machine, whose
`states` is a hand-written array. A test holds that line
(`jobMachine.states.length === JobStatusSchema.options.length`), and a test is weaker than a
compile error. Deriving `states` from the schema belongs to `state-machine.ts`, which is
`agent-contracts`' file.
