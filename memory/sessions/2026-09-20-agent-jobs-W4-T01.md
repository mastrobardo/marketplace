---
task:    W4-T01
agent:   agent-jobs
session: 2026-09-20T09:05Z
status:  closed
---

# Session — W4-T01

## Goal

The first half of the presupuestos funnel: a `Job` model, its contract, and the two states it can be
in before anyone quotes. Nothing in `W4` existed — there was no `Job` in `schema.prisma` at all.

## Current state

Landed and green on `W4-T01-job-posting`. contracts 23/23 · api routes 18/18 · api live 18/18 ·
whole api suite **405/405** under `STACK_LIVE=1` · `pnpm verify` 10/10.

- `Job` + `JobCategory` (`0010_job_posting`), rollback tested on a scratch database
- `packages/contracts/src/job.ts` — shapes, `jobMachine`, the publish guard
- `modules/jobs/{repository,routes}.ts`, four permission rows, wired in `app.ts` and `server.ts`

## Log

- 09:05 confirmed the ticket line understates the work: **no `Job` model exists**, so this carries
  the schema, the migration and the contract, not just routes.
- 09:08 found `state-machine.ts` already exists and is generic (*"owns no states of its own"*), so
  `Job` is its first consumer rather than a new mechanism.
- 09:12 operator: *"at least category need to be there. and might be multiple category"*. The second
  half changed the model — join table, not a column. Mirrored `ProviderCategory` exactly rather than
  inventing a second convention.
- 09:20 **the contract refused my design**: `defineMachine` rejected `terminal: []` because `OPEN`
  has no outgoing transition. The spec had argued *against* declaring it. The contract was right —
  within this ticket there is no way out of `OPEN`. Spec amended.
- 09:29 **insertion order does not exist.** Ordered `JobCategory` by `createdAt`; every row written
  in one transaction shares `CURRENT_TIMESTAMP`, so the tiebreak fell to a random uuid and the set
  shuffled between reads. Caught by AC8 on its first live run. Now taxonomy order (`position`,
  `slug`), which is what the picker shows anyway.
- 09:30 routes used bare `.parse()` → a bad body was a **500**. The repo's convention is `safeParse`
  plus an explicit `AppError`; a `ZodError` escaping a handler is mapped by nothing.

## Blocked / escalations

None. `STAGING_SEED_DEMO_PASSWORD` is still unset, but that is `W0-T30`'s handoff, not this one.

## Handoff

**Next action:** `W4-T02` — the rest of the machine. Two things it must do, both load-bearing:

1. **Remove `OPEN` from `jobMachine.terminal`** in the same change that gives it an exit. The list
   is honest today and becomes a lie the moment `AWARDED` exists.
2. **Move the "only a draft may be edited" rule out of `repository.update`.** It is a hand-written
   `if` that nothing ties to the machine's states — see the run record's self-assessment. With four
   more states it keeps working by accident, not by design.

Then `W4-T03` (quotes), which inherits the open question in spec §2.4: **a three-trade job — one
quote covering everything, or three covering parts?** The data model deliberately does not prejudge
it, and choosing wrong there is expensive.

**Do not redo:**
- The publish floor. One field, and it is the category set (`MEM-2026-09-20-4`).
- The many-to-many decision or the join-table shape — it mirrors `ProviderCategory` on purpose.
- Photos. Deferred by the operator; no column, no relation, and a nullable column nothing writes
  would be the empty promise this repo keeps refusing.
- `GET /api/jobs/me` pagination. Capped at 50 and unpaginated on purpose — add a cursor with the
  screen that needs one.
