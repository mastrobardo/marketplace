---
task:    W12-T16
agent:   agent-ui
session: 2026-09-12
status:  closed
---

# Session — W12-T16

## Goal
Land the spec for nightly visual regression + the axe pass over real routes, then — after the
operator approved the spec and said proceed — the implementation, on the same branch.

## Current state
Complete, pending review. Branch `W12-T16-visual-regression` off `main` at `bff80ed`, three
commits, PR #241.

- Spec + run record; run record carries the red phase and four findings.
- `packages/ui/visual/` — subjects, coverage, index-check, environment, config, probe, report,
  report-cli, serve.mjs, and the two Playwright specs. `playwright.config.ts`.
- 44 new tests in `packages/ui`, 10 in `tests/cd-workflows.test.ts`, 2 in `apps/web`.
- `.github/workflows/nightly-visual.yml` + `visual-baselines.yml`. `ci.yml` untouched, asserted.
- Q1 option A: `apps/web/src/shared/fault.ts`, stripped at resolve time.
- **No baselines committed** — deliberate (Q2 = fail). First nightly will be red until the
  baselines PR lands.

## Log
- Found `W12-T18` already merged (#240) — the local branch was stale and the project memory said
  "next is W12-T18". Precondition for this task ("runs after W12-T18") is therefore met.
- Chose `W12-T16` over `W12-T14` as the next task: T14 is unblocked but four of its five ACs are
  indexing ACs deferred by ADR-011 Amendment 1, so it would land with most of its stated value
  carved out. T13/T15 are priority-open for the same deferral; T19 is `[M]`/`[B]`. Operator
  confirmed.
- Enumeration source: `storybook-static/index.json`, not a source glob. Same artefact feeds the
  completeness gate, so the shooting list and the coverage check cannot disagree.
- **The central design decision**: the pinned list stays a file (some stories are genuinely unstable
  subjects), but its *completeness* is a per-PR test. A hand-written subject list is the failure
  class this repo has hit three times — the `database` job naming suites, `tokens.test.ts` AC6
  naming six pairs, AC4 walking `.css` only. Pinning stays deliberate; forgetting becomes red.
- Fingerprint file rather than a convention for "baselines only in the CI image". Mismatch **fails**
  in CI (a skipped required check counts as satisfied — `ci.yml` says so in its own comment) and
  **skips loudly** locally. Both directions asserted per `MEM-2026-09-11-22`.
- Two workflow files, because the baseline-regeneration path needs a write token and `ci.yml` is
  deliberately `contents: read`. AC16 pins that block as unchanged.
- Container image `mcr.microsoft.com/playwright:v1.63.0-noble` — 1.63.0 is what `pnpm-lock.yaml`
  resolves, so the image and the installed Playwright cannot drift apart.
- Deliberately did **not** guess Q1 and Q2. `W12-T18`'s §10 Q1 was written as "safe under either
  answer" and the implementation still got the answer's *contents* wrong, costing a re-cut. The
  lesson is that "safe to proceed" is not "safe to decide".
- Boundary: `.github/**` and `scripts/**` are `agent-devops`'. Declared in spec §5 following
  `W12-T06`'s precedent rather than re-litigated. This is the fourth W12 task to do it and T06's run
  record already asked for it to be settled once — raised as §10 Q3, filed rather than fixed (L10).

## Blocked / escalations
Both are in spec §10 verbatim. Neither blocks the spec PR; both block implementation.

- **Q1** — how the nightly reaches the 500 page. `W12-T09` correctly made it unreachable, so the one
  surface with no a11y coverage has no URL. Recommend option A: a build-time-stripped `?__boom=1`
  trigger reusing `W12-T08`'s resolve-time stripping. Blocks one row of AC11 only.
- **Q2** — missing baseline fails vs self-adopts. Recommend fail, with initial baselines landing
  through the regeneration workflow as this ticket's second PR.

## Log — implementation
- **The tolerance was the bug.** `maxDiffPixelRatio: 0.001` = 1,024 px on a 1280×800 shot; the AC19
  probe (1px padding on Card) moves 511. The gate could not fail on a single-component regression
  while every assertion about its *configuration* passed. Now `maxDiffPixels: 60`, absolute, with a
  test asserting the ratio form is absent. **This is the finding of the ticket.**
- The probe was wrong before the gate was: `padding-top` above a `padding` shorthand is a no-op.
  Verified `getComputedStyle` in the browser before trusting the second probe.
- Axe reporting zero violations is indistinguishable from axe never injecting — added
  `expect(results.passes.length).toBeGreaterThan(0)`.
- ESLint walks `storybook-static/` (gitignored, but ESLint does not read `.gitignore`): 21,128
  errors, latent since `W12-T03`. Ignored in `packages/ui/eslint.config.js`.
- `import.meta.glob` does not exist in Playwright. Split the equality into two cheap halves rather
  than dropping the property.
- All 7 axe routes pass, 500 page included.

## Handoff
**Next action**: PR #241 is spec + implementation. After it merges, run `visual-baselines.yml`
(workflow_dispatch, reason required) to land the initial baselines as its own PR — until then the
nightly is red **by design**, which is Q2 option A working, not a fault.

**Do NOT redo**: the research table above, and do not regenerate baselines locally — macOS
rasterisation is not the reference environment and committing them would poison the gate on its
first run. `visual-baselines.yml` is the only legitimate producer.

**Watch for**: the flake rate over the first fortnight (§10 Q4). If it is not near zero, shrink the
pinned list — do **not** raise `maxDiffPixels`. The ceiling assertion exists to make that awkward.
