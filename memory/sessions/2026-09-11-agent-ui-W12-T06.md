---
task:    W12-T06
agent:   agent-ui
session: 2026-09-11T16:12:00Z
status:  closed
---

# Session — W12-T06

## Goal
The workbench gets a URL: built and deployed per pull request and on merge, commented beside the app
preview, torn down when the PR closes. Last of the five tasks the operator queued.

## Current state
Branch `W12-T06-workbench-preview-deploy`, stacked on `W12-T04-story-a11y-gate` (PR #221) → #220 →
#219 → #218.

## Log
- 16:14 Corrected my own pre-flight assessment. I had told the operator T06 was mislabelled `[A]`
  and really `[M]`, because `wrangler pages deploy` prompts for a project that does not exist. The
  prompt is real; `wrangler pages project create` is not interactive, so the workflow can provision
  its own project with the token it already has. The human step survives only as a fallback if that
  token is scoped to `marketplace-web` alone.
- 16:18 `grep` broke AC29 ("every CLI a workflow runs is installed first"). Rewritten as `case` —
  which `deploy-preview.yml` already argues for in a comment I had read and not absorbed: grep exits
  1 on no match, so under `set -e` it is either an aborted job or a forbidden `|| true`.
- 16:22 The teardown's preflight never passed `pages-branch` through, although the guard script has
  always emitted it. Added to the job's outputs.
- 16:25 Found: **the teardown has never deleted a Cloudflare deployment at all** — Fly app and Neon
  branch only. Every `marketplace-web` preview ever built is still in the account. Not fixed (that
  is `agent-devops`' pipeline); written into spec §10 Q1.

## Handoff
Green locally: 83 assertions in `tests/cd-workflows.test.ts`, `pnpm verify` otherwise whole. Every
new `run:` block was parsed out of the YAML and checked with `bash -n`.

**It has never run.** The deploy needs the `preview` environment's credentials, which no agent may
hold, so the first evidence is this PR's own preview job. The two things most likely to be wrong:
Cloudflare's exact wording for an existing project (the `case` match), and whether the teardown's
`jq` filter matches the shape the Pages deployments API actually returns.

Next in `W12`: **T05** (three token layers, `[data-theme]`, a second theme — the toolbar from T03 is
already wiring the attribute onto both the story container and the document root), then **T07/T08**,
the search schema and the contract request that comes out of it.
