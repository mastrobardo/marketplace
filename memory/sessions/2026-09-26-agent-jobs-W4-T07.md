---
task:    W4-T07
agent:   agent-jobs
session: 2026-09-26T10:38Z
status:  closed
---

# Session — W4-T07

## Goal

The other half of the funnel: **a provider can find a job worth quoting on.**
`POST /api/jobs/:id/quotes` has existed since `W4-T03` and nothing lists the jobs, so every quote in
this system was written by somebody handed a uuid. This ticket is the feed — matched by category and
radius, with licence status **labelled rather than gated** — plus the screen that reads it and the
seed data that makes both demonstrable by a human who can sign in.

## Current state

Branch `W4-T07-provider-job-feed`, cut from `origin/main` (`02d3334`). Working tree was clean, no
open PR, and no open session file for this id — the `▶ NEXT` banner is trustworthy.

What exists to build on:
- `job_status_created_idx` is `(status, created_at DESC, id DESC)`; **no index orders OPEN jobs by
  `published_at`**, which is the order a feed wants.
- `search/repository.ts:142` is the geo statement with the operands the other way round.
- `job_category_category_idx` was added by `W4-T01` *for this ticket* (schema.prisma:419).
- The two accounts anybody can sign in as are `auth.demo-users`'. **`provider@marketplace.local`
  has no `ProviderProfile`**, so as shipped there is no account that can read a feed at all.
- `quotes.demo-comparison`'s job has **no address**, so it can never appear in a feed.

## Log

- 10:38 read `LONG_TERM.md`, `slices/agent-jobs.md`, `sessions/…-W4-T04.md`, the `▶ NEXT` banner,
  and the code the ticket touches: `modules/{jobs,quotes,search}/*`, `contracts/{job,quote,pagination}.ts`,
  `auth/permissions.ts`, `prisma/schema.prisma`, `prisma/seed/*`, `apps/web/src/routes/{jobs,job}.tsx`,
  `features/quotes/ranking.ts`, `shared/{api,query}.ts`, `i18n/locales/es.ts` (see 14:40).

- 10:40 **two operator decisions, taken before any code** (verbatim, 2026-09-26): *"Agree with the
  labels proposal: in the view will be the client to be able to filter"* and *"Yes, include the
  screen as well. Also, WITHOUT COMMITTING IT (u can gitignore), a list of accounts we have in the
  seeder to be able to test it easily (easier for me to just follow a txt)"*.
  So: the API **labels**, the screen **filters**, and the branch carries an untracked credentials
  file for manual testing.

## Blocked / escalations

None.

- 11:20 **spec, contract, migration `0015`, then red.** Contracts were frozen first (pipeline step 2)
  so `packages/contracts` was green before anything else ran; the API files did not import at all and
  the web suite failed on `/es/feed` not being a route. Both pastes are in the run record.

- 12:40 **`text > uuid`, and only page two says so.** The statement selected `j.id::text` in the CTE,
  so the keyset compared text against a `::uuid` cursor and Postgres refused outright. Page one was
  green throughout. Caught by AC10/AC11, which assert over the **union** of two pages rather than
  their lengths — a length assertion would have passed, because there was no second page.
  `MEM-2026-09-26-3`.

- 12:55 **a backtick inside a `Prisma.sql` template is a parse error.** The SQL comment explaining the
  fix above used backticks the way every TypeScript comment here does, and the file stopped compiling.

- 13:30 **the seeded provider is a real provider.** `jobs.demo-feed` gives the sign-in-able account a
  `ProviderProfile`, so a Madrid search returns six rather than five. Four assertions in two other
  slices' suites moved: three counts updated with a comment naming both sources, and `seed-live`'s
  AC9 became `toBeGreaterThanOrEqual(5)` because its subject is *whatever is seeded has a profile page
  that serves* and a literal never carried that.

- 13:50 **the whole `apps/api` suite green live** — 571 passed, including `db.test.ts` AC10's
  reverse-order rollback over `0015` and AC12's drift check. An earlier run showed twelve failing
  files and every one of them was a missing `BETTER_AUTH_SECRET`/`DATABASE_URL`, not a defect: CI sets
  them, a bare `STACK_LIVE=1` does not.

## Blocked / escalations

None.

## Handoff

**Next action:** the two forms the funnel still has no UI for, which the `▶ NEXT` banner now names in
place of a task id, because filing them is the operator's call:

1. **a client cannot post a job from the browser** — `W4-T01`'s tail, and now the more visible half:
   the feed has readers and only a seeder puts jobs in front of them;
2. **a provider cannot send a quote from the browser** — `W4-T03`'s tail, deliberately not taken here
   (spec §6), which is why a feed row links to nothing it cannot deliver.

Alternatively `W5-T02`, still the unblocker for `W4-T05` and `W4-T09`, still `[A]` `[B]`.

**Three things `W4-T07` learned that whoever takes those forms needs:**

1. **The demo world now has a professional you can sign in as.** `provider@marketplace.local` has a
   profile, trades and a radius, and `client@marketplace.local` has a default address — so a posting
   form has somewhere to infer from and a quote form has somebody to send as. `demo-accounts.local.txt`
   (untracked, `*.local.txt`) is the crib, and it is hand-written: if it disagrees with the seeders,
   they are right.
2. **A nullable column can still be a sort key when the endpoint's own filter guarantees it is set.**
   `published_at` is nullable and pages fine, because `WHERE status = 'OPEN'` is both the feed's filter
   and the condition that makes it non-null — stated once, in the index's predicate
   (`MEM-2026-09-26-2`). `ratingAvg` was ruled out twice for the opposite reason; check before ruling.
3. **This slice has now said *state the fact, leave the choice* three times** — coverage (`W4-T03`),
   the untouched siblings (`W4-T04`), the licence label (`W4-T07`). A fourth ticket that wants to
   *hide* something should expect to argue for it.

**Do not redo:**
- **The licence label.** It is an operator decision of 2026-09-26 *and* a data fact: nothing in this
  schema knows who holds a licence until `W8`. The charter still says "gating" and is deliberately
  **not edited** — `agents-drift` compares it with `.claude/agents/`, and rewriting it is the
  operator's.
- **The absence of server-side filters.** Same decision, same date: the filtering is the view's.
  §2.6 names the trigger for revisiting it.
- **`publishedAt` as the paging key**, and `0015` with it.
- **The absence of a quote form, a provider-side job detail page, and navigation to `/es/feed`.** All
  three are named in spec §6 with their owners.
- **The absence of an axe pass.** No harness here reaches a page behind a session — `W12-T04` runs
  over Storybook, `W12-T16` over public routes — exactly as for `W4-T04`'s screens
  (`MEM-2026-09-20-31`).

- 14:40 **rebased onto `W12-T21`, which landed while this branch was open** (#285, operator merged it
  2026-09-26). It turns the catalogues into data: `es.ts`/`en.ts` are now `es.json`/`en.json`, and the
  compile-time `TranslationKey` union is **gone on purpose** — a union derived from a snapshot is true
  until the first fetch from a translation service, which is the premise of the move. The only
  conflict was the two catalogues, modify-against-delete; the thirty-two `feed.*` keys moved into the
  JSON unchanged, before `footer.rights` where both files end. Nothing in `feed.tsx` had to change:
  a template-literal key like `feed.sort.` + the option needed the union to typecheck and now needs
  nothing, and the runtime
  guard is stricter than what it replaced — `missingKeyHandler` **throws**, so a key this screen uses
  and neither catalogue has fails a test rather than rendering its own name.
  The web suite is 294 rather than 297 because that ticket removed three compile-guard fixtures.

**What a reviewer should push back on** (also in the run record): the hand-written projection in
`feed-repository.ts`, where the outbound `strictObject` parse is the only thing between a widened
`SELECT` and somebody's address; whether `0015`'s index earns its place when the planner prefers the
GiST index on a narrow radius; and the four count assertions changed in `seed-live.test.ts` and
`categories-live.test.ts`, which are another slice's tests.
