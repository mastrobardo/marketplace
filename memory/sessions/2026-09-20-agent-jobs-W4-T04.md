---
task:    W4-T04
agent:   agent-jobs
session: 2026-09-20T18:05Z
status:  open
---

# Session — W4-T04

## Goal

The client's side of the presupuesto: **compare the quotes on your job, and say yes or no to one.**
`ACCEPTED` and `REJECTED` — the two states `W4-T03` deliberately did not declare — arrive here with
the routes that can produce them, plus the screen that reads them and the cursor the screen needs.

## Current state

Branch `W4-T04-quote-comparison`, cut from `origin/main` (`13fb39b` + `4871638`). **Nothing is
open** — `gh pr list` is empty and `memory-format-check` is byte-identical to `main`, so the
`▶ NEXT` banner is trustworthy this time, which `MEM-2026-09-20`'s handoff-lag warning says to check
rather than assume.

`W4-T03` shipped: `quote` table, two states, four routes, coverage computed at read time.

## Log

- 18:05 read `LONG_TERM.md`, `slices/agent-jobs.md`, `sessions/…-W4-T03.md`, ADR-013/014/015,
  `packages/contracts/src/quote.ts`, `0012_quote_submission/migration.sql`.

- 18:10 **`MEM-2026-09-20-22` states its own consequence backwards.** It says a rejected quote would
  block a resubmission *"under the current predicate"*. It would not: the index is
  `WHERE status = 'PENDING'`, so a `REJECTED` row falls out of it and the provider is free to quote
  again immediately. The decision the entry asks for is still needed — the default just runs the
  other way from the one it warns about. Corrected in this PR.

- 18:15 **three operator decisions**, all confirming the recommendation put to them:
  1. **UI scope** — API + comparison screen + seeder. No job-posting form; a client still cannot
     create a job from the browser, and that stays `W4-T01`'s tail rather than becoming this
     ticket's.
  2. **Rejection frees the slot.** The predicate stays `WHERE status = 'PENDING'`. A provider who is
     told no may send a revised price; being re-pitched is the cost, and no rate limit exists.
  3. **Siblings stay `PENDING`.** One `ACCEPTED` per job, enforced by a second partial unique index.
     The job stays `OPEN`. Nothing is burned while `W4-T05`'s payment can still fail.

- 18:20 **the seam question `MEM-2026-09-20-14` left open was already answered by ADR-013 the same
  day it was asked.** Operator, 2026-09-20: *"BOTH. Booking and job should reflect the same state.
  This will be also a way to check if smtg is going badly"* — which is ADR-013 §1 (two of the three
  sources of truth) and §2 (*"reconciled rather than coupled"*, a reconciler writes a **finding**
  when they disagree, `W5-T11` gaining a second dimension). The entry, the `▶ NEXT` banner and
  `W4-T05`'s inherited question are all stale on `main`; all three corrected here.
  **It changes no code in this ticket**: `IN_PROGRESS`/`COMPLETED` still have nothing that can
  produce them, so adding them now would be `MEM-2026-09-20-29`'s empty promise.

## Blocked / escalations

None.

## Handoff

<open — this session is in progress>
