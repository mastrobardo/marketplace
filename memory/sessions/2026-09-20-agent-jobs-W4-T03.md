---
task:    W4-T03
agent:   agent-jobs
session: 2026-09-20T16:20Z
status:  closed
---

# Session — W4-T03

## Goal

Quotes — *presupuestos* — and the question `W4-T01` §2.4 deliberately refused to prejudge, now
answered by the operator: **one quote covers the whole job**.

## Current state

Branch `W4-T03-quote-submission`, **stacked on `W4-T02-job-state-machine` (PR #277, green and
awaiting its two reviewers)** rather than on `main`. Not a preference: `canQuoteOn` is a
`Record<JobStatus, boolean>` and `JobStatus` on `main` has no `CANCELLED` yet, so the file does not
compile there. The dependency is real, and the `Record` is what surfaced it — `tsc` refused the
branch before any test did.

## Log

- 16:20 operator answers §2.4: **one quote for everything.** *"Usually, a plumber knows an
  electirician wich works with him already, or has a small comapny with all profiles. It would be
  also too chaotic for a user accepting X presupuestos […] It is up to the professional to decide
  to apply or not."* So the platform does **not** gate quoting on category coverage.
- 16:25 raised the licence consequence: one quote over three trades makes "quoting a gated trade you
  are not verified for" the normal case, not an edge. Operator: *"i might not be verified as an
  electrician, but given my rates and past jobs recorded is up to the client to pick me or not.
  Nothing enforces, but stated clearly."* → the quote carries coverage against the job's categories.
- 16:35 **checked the schema before designing that field, and it changed the design.** There is no
  provider-verification data anywhere — `Category.requiresLicence` marks the *trade*, and nothing
  records a provider being verified for it (`W8-T01`/`W8-T02`, unbuilt). So "verified: plumbing ·
  not verified: electrical" cannot be said truthfully today. What *is* true: whether the provider
  **lists** the category, and whether the category requires a licence. Coverage is built from those
  two, and carries **no** `verified` field — a nullable one nothing writes is the empty promise
  `W4-T01` refused for photos.

- 17:05 **deviation, and it improves the refusal:** a `DRAFT` job answers `404` rather than the
  `CONFLICT` §5 first specified. A draft is invisible to a non-owner (`W4-T01` §3), so a 409 naming
  its state confirms a private row exists. A `CANCELLED` job keeps the 409 — the provider saw it in
  the feed, they were slow, not snooping. Visibility is a query concern; quotability is a state rule.
- 17:20 whole api suite 480/480 under `STACK_LIVE=1`, contracts 283.

## Blocked / escalations

None.

## Handoff

**Next action:** `W4-T04` — quote comparison and accept/reject. It owns `ACCEPTED` and `REJECTED`,
the two states this ticket deliberately did not declare.

**Two things to decide there before writing code:**

1. **The partial unique index changes meaning when those states arrive.**
   `WHERE status = 'PENDING'` means a withdrawn quote frees the slot today. Decide explicitly
   whether a *rejected* quote should block a resubmission — under the current predicate it would,
   silently (`MEM-2026-09-20-22`).
2. **`listForJob` has no limit and no cursor.** Capped nowhere, on the assumption a job attracts a
   handful of quotes. `W4-T04` builds the screen that reads it, so the page size stops being a guess
   there. It is this ticket's self-assessed weakest part.

**Do not redo:**
- One quote for the whole job. No per-category price, no per-trade quote rows (`MEM-2026-09-20-21`).
- The absence of a `verified` field on coverage. `W8` adds it when it can populate it.
- Expiry as arithmetic. No `EXPIRED` column, no sweeper.
- `DRAFT` → 404 / `CANCELLED` → 409. It is two rules, on purpose.

**Branch state:** stacked on `W4-T02-job-state-machine` (PR #277, green, awaiting two reviewers).
`canQuoteOn` will not compile without `CANCELLED`, so this cannot be rebased onto `main` until #277
lands.
