# Repo memory — gotchas

Traps found the hard way. Each record must carry `evidence` — a PR, a failing test, an incident, an
intervention id. **No speculation.** If it has not actually bitten us, it belongs in a spec's risk
section, not here.

One record per file, named for its id. See `../../_RECORD_TEMPLATE.md`.
