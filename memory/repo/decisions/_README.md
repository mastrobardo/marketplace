# Repo memory — decisions

Non-obvious constraints that shape the code. Formal architecture decisions live in `docs/adr/`;
these records hold the *why* an agent would otherwise have to rediscover.

One record per file, named for its id. See `../../_RECORD_TEMPLATE.md`.
